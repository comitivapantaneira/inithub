# ---------- General ------------
LOGGING_LEVEL="info"
BACKEND_URL="http://localhost:8000"

# ---------- Database -----------
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="inithub"
# Prisma expects a standard Postgres URL
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}"

# ---------- Backend ------------
# Optional: required only if you want embeddings features
OPENAI_API_KEY="your-openai-key-here"
OPENAI_MODEL_NAME="gpt-4.1-2025-04-14"

# ---------- Agent --------------
# OpenRouter configuration used by the Agent (FastAPI)
OPENROUTER_API_KEY="your-openrouter-key-here"
OPENROUTER_MODEL_NAME="openai/gpt-4.1-nano"
OPENROUTER_ENDPOINT="https://openrouter.ai/api/v1"

# ---------- Frontend -----------
# WebSocket URL to reach the Agent from the browser.
# Defaults internally to ws://<host>:8000/ws/v1/agent if not set.
VITE_AGENT_WS_URL="ws://localhost:8000/ws/v1/agent"
